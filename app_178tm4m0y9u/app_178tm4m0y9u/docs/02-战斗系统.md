# 战斗系统设定说明书

> 本文档基于 `src/js/battle.js`、`src/js/config.js`、`src/js/ui.js` 与 `src/index.html` 的实际代码撰写，描述梦幻西游风格回合制宠物战斗游戏的完整战斗机制。
> 所有数值、公式、字段名均与代码保持一致，未做臆造。

---

## 目录

1. [系统概述](#1-系统概述)
2. [回合制战斗机制](#2-回合制战斗机制)
3. [速度排序系统（buildTurnQueue）](#3-速度排序系统buildturnqueue)
4. [战斗时序机制](#4-战斗时序机制)
5. [战斗界面 UI](#5-战斗界面-ui)
6. [多怪物波次系统](#6-多怪物波次系统)
7. [怪物生成机制（getLayerMonster）](#7-怪物生成机制getlayermonster)
8. [怪物种族与技能](#8-怪物种族与技能)
9. [怪物强化与地图削弱](#9-怪物强化与地图削弱)
10. [伤害公式](#10-伤害公式)
11. [暴击系统](#11-暴击系统)
12. [buff/debuff 系统](#12-buffdebuff-系统)
13. [状态系统](#13-状态系统)
14. [被动技能触发机制](#14-被动技能触发机制)
15. [光环技能机制](#15-光环技能机制)
16. [法术群体技能 targetCount 系统](#16-法术群体技能-targetcount-系统)
17. [战斗速度设置](#17-战斗速度设置)
18. [自动战斗](#18-自动战斗)
19. [主线宝箱掉落（dropChests）](#19-主线宝箱掉落dropchests)
20. [天赋额外掉落](#20-天赋额外掉落)
21. [战斗日志与掉落日志系统](#21-战斗日志与掉落日志系统)
22. [副本战斗（isDungeon）](#22-副本战斗isdungeon)
23. [藏宝图战斗（isTreasure）](#23-藏宝图战斗istreasure)
24. [竞技场战斗（isArena）](#24-竞技场战斗isarena)
25. [动画系统](#25-动画系统)
26. [参数公式速查表](#26-参数公式速查表)

---

## 1. 系统概述

战斗系统以 `liveBattle` 全局对象为核心状态机，由以下源文件协作实现：

| 文件 | 职责 |
| --- | --- |
| `src/js/battle.js` | 战斗核心逻辑：回合调度、伤害计算、技能释放、波次推进、奖励发放 |
| `src/js/config.js` | 静态配置：地图、Boss、怪物种族技能、宝箱掉落、天赋掉落、副本定义 |
| `src/js/ui.js` | 战斗渲染：`renderBattleArena()`、技能释放公告 `showSkillCastAnnouncement()`、速度/挂机按钮 |
| `src/index.html` | CSS 样式：`.battle-arena`、`.battle-log`、`.speed-btn`、各类动画关键帧 |

战斗整体流程为：`startLiveBattle()` → `spawnMonster()` → `buildTurnQueue()` → `scheduleNextTurn()` → `executeTurn()` → 行动者执行 → `nextFromQueue()` → 循环直至一方全灭 → `advanceWaveOrEndBattle()` → `processWaveCleared()`。

---

## 2. 回合制战斗机制

### 2.1 回合定义

每一回合（round）由 `buildTurnQueue()` 构建，将所有存活的玩家宠物与所有存活怪物加入行动队列，然后按速度降序排序。回合开始时打印日志：

```
── 第 N 回合 ──
```

### 2.2 行动执行流程（executeTurn）

`executeTurn()` 是核心调度函数，逻辑如下：

1. 若 `liveBattle.animating === true`，直接返回（防止重入）。
2. 若 `turnQueue` 为空：
   - 调用 `applyTurnRegen()` 处理回合结束再生（持续恢复、Hot、DoT、buff 回合递减、技能冷却递减）。
   - 若所有怪物已死（DoT 击杀），记录击杀日志后调用 `advanceWaveOrEndBattle()`。
   - 若所有宠物已死，调用 `processBattleDefeat()`。
   - 否则调用 `buildTurnQueue()` 开启新回合。
3. 从 `turnQueue` 队首 `shift()` 取出当前行动者。
4. 根据行动者类型调用 `executePetAction(pet)` 或 `executeMonsterAction(monsterIdx)`。
5. 行动完成后由行动函数内部调用 `nextFromQueue()` 推进下一名行动者。

### 2.3 宠物行动（executePetAction）

宠物行动按以下顺序判定：

| 优先级 | 判定项 | 说明 |
| --- | --- | --- |
| 1 | 状态阻止 | `stunned`/`frozen`/`sleeping`/`rooted` 任一 > 0 则跳过本回合，状态回合数 -1 |
| 2 | 光环技能 | `tryExecuteAuraSkills(pet)`：无冷却且未生效的光环当前回合必释放 |
| 3 | 主动技能 | `tryUseActiveSkill(pet)`：按概率触发，受魔法值与冷却限制 |
| 4 | 普通攻击 | `executeNormalAttack(pet)`：默认行动 |

### 2.4 怪物行动（executeMonsterAction）

| 优先级 | 判定项 | 说明 |
| --- | --- | --- |
| 1 | 死亡检查 | HP ≤ 0 跳过 |
| 2 | 状态阻止 | 同宠物（眩晕/冻结/睡眠/定身） |
| 3 | 主动技能 | 50% 概率使用 `monster.skills` 中随机一个主动技能（无需魔法值） |
| 4 | 普通攻击 | 50% 概率，按阵位顺序攻击首个存活宠物 |

---

## 3. 速度排序系统（buildTurnQueue）

`buildTurnQueue()` 负责构建每回合的行动顺序：

```javascript
actors.sort(function(a, b) { return b.speed - a.speed; });
```

### 3.1 行动者来源

| 行动者类型 | 速度来源 |
| --- | --- |
| 宠物 | `getPetStats(pet).速度`（默认 10） |
| 怪物 | `monster.speed`，生成时设为 `Math.floor((level || 1) * 2 + 10)` |

### 3.2 排序规则

- **降序排列**：速度高的先出手。
- 同速时由 JavaScript `Array.sort` 稳定性决定（V8 引擎稳定排序，按入队顺序执行）。
- 已死亡的单位不会入队（HP > 0 才加入）。
- 每回合重新构建队列（`liveBattle.round++`）。

---

## 4. 战斗时序机制

### 4.1 单位动画隔离

战斗采用「仅当前出手单位动画，其他不动」的设计：

- 全局 `liveBattle.animating` 标志位：行动开始置 `true`，行动结束（`nextFromQueue`）置 `false`。
- `executeTurn()` 在 `animating === true` 时直接返回，避免动画期间被打断。
- 攻击/技能动画通过 `setTimeout` 延迟推进（普攻 400ms，技能 400ms）。

### 4.2 回合延迟

`scheduleNextTurn()` 通过 `setTimeout` 在 `getTurnDelay()` 毫秒后触发下一次 `executeTurn()`。延迟数值见 [第 17 节 战斗速度设置](#17-战斗速度设置)。

### 4.3 关键时序常量

| 时序点 | 延迟 | 代码位置 |
| --- | --- | --- |
| 普攻/技能后推进下一行动 | 400ms | `setTimeout(() => nextFromQueue(), 400)` |
| 连击追加攻击 | 400ms | 二段/三段连击 |
| 自动战斗下一波生成 | 1000ms（主线）/ 1500ms（失败） | `battleSpawnTimer` |
| 技能释放公告显示 | 1500ms | `showSkillCastAnnouncement` |

---

## 5. 战斗界面 UI

### 5.1 容器结构

战斗场景由 `renderBattleArena()`（`ui.js:1144`）渲染，外层 `.battle-arena`，内层 `.battle-scene`：

```html
<div class="battle-scene ..." style="background:linear-gradient(180deg,#1a1a2e 0%,#16213e 40%,#0f3460 100%);min-height:440px;">
  <div id="skillCastAnnouncement" class="absolute inset-0 ..."></div>
  <!-- 怪物区（右上） -->
  <!-- 宠物区（左下） -->
  <!-- 关卡进度条 -->
</div>
```

### 5.2 关键尺寸

| 属性 | 值 | 说明 |
| --- | --- | --- |
| `.battle-scene` 最小高度 | **440px** | `min-height:440px` |
| `.battle-arena` 最小高度 | 280px | CSS 兜底，实际由内层 440px 撑开 |
| `#battleArenaContent` 最小高度 | 380px | 占位高度 |
| `.battle-log` 最大高度 | 180px | 可滚动 |

### 5.3 卡片布局

玩家宠物与敌方怪物采用**一致的纵向卡片布局**（`flex flex-col items-center`），整体水平排列（`flex flex-wrap gap-2`）：

- **敌方怪物区**：位于场景右上方，宽度 55%，`justify-end` 靠右对齐。
- **玩家宠物区**：位于场景左下方，宽度 50%。

每张卡片内部纵向结构（自上而下）：

1. 种族图标（emoji，如 🐉😈👼👺🧝🟢）
2. 状态图标（左上角，如 ❄️🔥☠️💫😴🌿）
3. 当前出手标记（宠物 ⚔️ 图标）
4. 名称（按品质/类型着色）
5. 等级 + 阵位/类型标签
6. HP 数值与血条（HP > 50% 绿色 / 25–50% 黄色 / < 25% 红色脉冲）
7. buff/debuff 徽章

### 5.4 怪物卡片类型标识

| 类型 | 图标 | 标签 | 颜色 |
| --- | --- | --- | --- |
| mob（小怪） | 👹 | （无） | `#94a3b8` 灰 |
| elite（精英） | ⭐ | 精英 | `#f59e0b` 橙 |
| boss（首领） | 👑 | BOSS | `#ef4444` 红 |

### 5.5 顶部状态条

场景顶部右侧显示：

```
回合 R{round} · 敌人 {aliveCount}/{monsters.length}
```

底部进度条显示地图名、当前层/总层数、阶段（小怪 N/M / 精英战 / BOSS战 / 已通关）。

---

## 6. 多怪物波次系统

### 6.1 波次生成（generateMonsterWave）

`generateMonsterWave(mapId)` 根据当前关卡阶段 `prog.phase` 生成多怪物波次：

| 阶段 | 怪物组成 | 总数 |
| --- | --- | --- |
| `mobs`（小怪关） | 3–6 个小怪 | `randomInt(3,6)` |
| `elite`（精英关） | 4 小怪 + 2 精英（精英最后出场） | 6 |
| `boss`（Boss 关） | 3 小怪 + 2 精英 + 1 Boss（Boss 最后出场） | 6 |

### 6.2 上限限制

```javascript
if (monsters.length > 6) monsters = monsters.slice(0, 6);
```

**最多 6 个敌人同时上场**，超出部分截断。

### 6.3 关卡进度推进（processWaveCleared）

每清空一波怪物后，根据本波中「最高等级类型」推进关卡进度（优先级 boss > elite > mob）：

| 当前阶段 | 击杀后行为 |
| --- | --- |
| `mobs` | `mobsKilled++`；达到 `mobsNeeded`（8–12 随机）后切到 `elite` 阶段 |
| `elite` | 若 `currentLayer >= map.layers` → 切到 `boss` 阶段；否则 `currentLayer++`，重置为 `mobs` 阶段 |
| `boss` | `phase = 'cleared'`，解锁下一张地图 |

### 6.4 特殊战斗例外

副本与藏宝图战斗**不使用波次系统**，保持单怪模式：

```javascript
if (liveBattle && (liveBattle.isDungeon || liveBattle.isTreasure)) {
  monsters = [getLayerMonster(G.player.currentMap)];
}
```

---

## 7. 怪物生成机制（getLayerMonster）

`getLayerMonster(mapId, forceType)` 是怪物生成的核心函数。

### 7.1 类型判定

- 若传入 `forceType`，直接使用该类型（用于波次系统生成混合波次）。
- 否则按 `prog.phase` 判定：`mobs` → `mob`，`elite` → `elite`，`boss` → `boss`。

### 7.2 倍率与配置

| 类型 | `hpMult` | `atkMult` | `defMult` | 配置来源 |
| --- | --- | --- | --- | --- |
| mob（小怪） | 5 | 5 | 1.2 | `map.monsters` + `MONSTER_RACE_SKILLS[race].mob` |
| elite（精英） | 12 | 8 | 1.6 | `map.eliteName` + `MONSTER_RACE_SKILLS[race].elite` |
| boss（首领） | 24 | 12 | 2.2 | `MAP_BOSSES[mapId]`（固定配置） |

> 代码注释中描述「怪物强度强化：小怪×5，精英×4，Boss×3（整体强度提升）」为整体强化概念；实际数值以上表 `hpMult/atkMult` 为准。

### 7.3 属性计算公式

连续成长曲线（避免跨图难度跳跃）：

```
lvScale = 1 + lv × 0.012
baseHp  = floor((40 + lv × 20) × hpMult × lvScale) ± 5% 随机
baseAtk = floor((4 + lv × 3.8) × atkMult × lvScale) ± 10% 随机
def     = floor(lv × 2.0 × defMult)
```

其中 `lv = randomInt(map.minLv, map.maxLv)`。

### 7.4 起源草地削弱

地图 1（起源草地）整体强度削弱一半：

```javascript
if (mapId === 1) {
  baseHp  = Math.floor(baseHp * 0.5);
  baseAtk = Math.floor(baseAtk * 0.5);
}
```

### 7.5 速度生成

怪物生成后若未设置 `speed`，则：

```
speed = floor((level || 1) × 2 + 10)
```

---

## 8. 怪物种族与技能

### 8.1 种族技能组合（MONSTER_RACE_SKILLS）

定义于 `config.js:1278`，每种种族有 mob / elite 两套标准技能组合：

| 种族 | mob 主动 / 被动 | elite 主动 / 被动 |
| --- | --- | --- |
| 龙 | 烈焰击/雷霆击/致命一击 · 力量强化/暴击 | 烈焰击/致命一击/陨石/双重斩 · 力量强化2/吸血 |
| 史莱姆 | 铁壁/守护盾/烈焰击 · 防御强化/再生 | 铁壁/守护盾/反击姿态/荆棘之盾 · 防御强化2/招架 |
| 哥布林 | 偷袭/毒牙/毒雾 · 偷袭/闪避 | 偷袭/毒牙/毒雾/缠绕 · 偷袭2/毒攻 |
| 天使 | 雷霆击/震击/圣光 · 速度强化/幸运 | 雷霆击/震击/圣光/加速 · 速度强化2/法心 |
| 精灵 | 冰箭/冰冻/治疗 · 法心/再生 | 冰箭/冰冻/暴风雪/治疗 · 法心2/法连 |
| 恶魔 | 暗影击/血液汲取/沉默 · 吸血/法心 | 暗影击/末日审判/血液汲取/沉默 · 吸血2/法心2 |

### 8.2 地图种族池（MAP_MONSTER_RACES）

每张地图有固定种族池（小怪/精英从中随机）：

| 地图 ID | 名称 | 种族池 |
| --- | --- | --- |
| 1 | 起源草地 | 史莱姆、哥布林 |
| 2 | 新手森林 | 精灵、哥布林 |
| 3 | 幽暗矿洞 | 史莱姆、哥布林 |
| 4 | 亡灵墓地 | 恶魔、史莱姆 |
| 5 | 烈焰火山 | 龙、恶魔 |
| 6 | 冰霜高原 | 龙、天使 |
| 7 | 暗影沼泽 | 恶魔、哥布林 |
| 8 | 天空之城 | 天使、龙 |
| 9 | 深渊裂隙 | 恶魔、精灵 |
| 10 | 龙之巢穴 | 龙、恶魔 |
| 11 | 终焉神殿 | 恶魔、龙 |

### 8.3 Boss 固定配置（MAP_BOSSES）

Boss 使用 `MAP_BOSSES[mapId]` 固定配置，包含 `name`、`race`、5 个主动技能、3 个被动技能、初始 buff 与专属战利品。

下表为各 Boss 一览（buff 字段中 atk/def/spd/all 为百分比加成，reflect 为反伤比例）：

| 地图 | Boss 名称 | 种族 | buffs | 专属 loot |
| --- | --- | --- | --- | --- |
| 1 | 草原因子 | 史莱姆 | atk+10%, def+50%, all+10%, reflect 30% | hatch_stone |
| 2 | 树精之王 | 精灵 | atk+20%, def+30%, all+15% | fusion_stone |
| 3 | 暗影矿主 | 哥布林 | atk+40%, spd+30%, all+10% | exp_ticket |
| 4 | 巫妖之王 | 恶魔 | atk+50%, def+30%, all+20% | gold_ticket |
| 5 | 炎魔领主 | 龙 | atk+50%, def+20%, all+15% | egg_ticket |
| 6 | 冰龙女王 | 龙 | atk+40%, def+40%, all+15% | egg_ticket |
| 7 | 暗影之主 | 恶魔 | atk+50%, spd+30%, all+20% | skill_ticket |
| 8 | 天空之神 | 天使 | atk+50%, spd+40%, all+25% | forge_ticket |
| 9 | 混沌魔王 | 恶魔 | atk+60%, def+30%, all+30% | map_ticket |
| 10 | 万龙之祖 | 龙 | atk+70%, def+40%, all+30% | gem_ticket |
| 11 | 创世神 | 恶魔 | atk+80%, def+50%, all+40% | guixu_pill |

### 8.4 Boss 初始 buff 永久生效

Boss 战斗开始时，`spawnMonster()` 自动将 `bossConfig.buffs` 写入 `monsterBuffsArray`，并以 `9999` 回合表示「永久生效」：

```javascript
if (buffs.atk)  { b.atk = buffs.atk;  b.buffTurns.atk = 9999; }
if (buffs.reflect) { b.reflectBuff = buffs.reflect; b.reflectTurns = 9999; }
// def / spd / all 同理
```

---

## 9. 怪物强化与地图削弱

### 9.1 怪物强化倍率

参见 [第 7.2 节](#72-倍率与配置)：

| 类型 | HP 倍率 | 攻击倍率 | 防御倍率 |
| --- | --- | --- | --- |
| 小怪 | ×5 | ×5 | ×1.2 |
| 精英 | ×12 | ×8 | ×1.6 |
| Boss | ×24 | ×12 | ×2.2 |

### 9.2 副本难度强化

副本/团本使用独立强化倍率（见 `processDungeonWaveComplete` 与 `enterTeamDungeon`）：

| 副本类型 | HP 倍率公式 |
| --- | --- |
| 普通副本（单怪） | `(40 + lv×20) × 5 × lvScale`（小怪×5） |
| 团本 Boss | `(40 + lv×20) × 3 × 4 × lvScale`（即精英级 × 团本倍率） |
| 团本攻击 | `(4 + lv×3.8) × 2 × 4 × lvScale` |
| 副本防御 | `floor(lv × 2.0 × 1.2 × 1.5)` |

### 9.3 起源草地削弱

地图 1 的 `baseHp` 与 `baseAtk` 在生成后乘以 0.5（向下取整），相当于所有怪物属性减半，作为新手地图保护机制。

---

## 10. 伤害公式

### 10.1 通用伤害公式

```
基础伤害 = 攻击力来源 × 技能系数(dmgPct) × 技能伤害倍率(skillDmgMult) × (1 + 天赋法术穿透)
        − 目标防御 × (0.6 − 无视防御比例)
最终伤害 = round(基础伤害 × 随机波动(0.90~1.10)) × 暴击倍率（若暴击）
伤害下限 = max(1, 最终伤害)
```

### 10.2 攻击力来源

| 技能类型 | 攻击力来源 |
| --- | --- |
| 物理技能 | `getPetAtkPower()`：`stats.攻击力` × 各类加成 |
| 法术技能 | `getPetSpiritPower()`：`stats.灵力` × 各类加成 |
| 怪物法术技能 | `getMonsterAtkPower() × 1.2` |

### 10.3 防御减免系数

代码注释明确：**防御削减系数 0.7 → 0.6**（让防御收益降低，提升伤害体感）：

```
有效防御 = 目标防御 × (1 − defReduced)        // defReduced 来自破甲状态
减免后伤害 = 攻击力 × dmgPct × skillDmgMult × (1 + skillDmgBonus) − 有效防御 × (0.6 − ignoreDefPct)
```

### 10.4 技能伤害倍率（skillDmgMult）

```
skillDmgMult = 1 + passives.skillDmg
```

### 10.5 真实伤害

若 `skill.trueDmg` 为真，则**无视防御**：

```
rawDmg = power × dmgPct × skillDmgMult × (1 + skillDmgBonus)
```

### 10.6 多段技能（hits）

若技能有 `hits` 字段（如连击技能），按 `decayPerHit` 衰减：

```
第 i 段伤害 = round(单段伤害 × (1 - decayPerHit)^i)
```

### 10.7 斩杀机制

若 `skill.executeThreshold` 存在且目标当前 HP 低于阈值：

```
伤害 = round(伤害 × (skill.executeMult || 2))
```

### 10.8 装备特效绝杀

`execute` 特效对**小怪**有 5% 概率秒杀（对精英/Boss 无效）：

```javascript
if (equipSpecials.indexOf('execute') >= 0 && monster.enemyType === 'mob' && Math.random() < 0.05) {
  damage = liveBattle.monsterHpArray[targetIdx]; // 直接造成剩余 HP 伤害
}
```

---

## 11. 暴击系统

### 11.1 暴击率组成

```
critChance = 0.10（基础）
           + passives.critRate          // 被动暴击率
           + auras.teamCrit             // 光环暴击率
           + bloodline.effects.critRate // 血统暴击率
           + skill.bonusCrit            // 技能额外暴击率
           + stats.critRate             // 人物暴击率（20% 附加给宠物）
```

判定方式：`isCrit = Math.random() < critChance`

### 11.2 暴击伤害倍率

```
critMult = 1.8（基础）
         + passives.critDmg    // 被动暴击伤害
         + stats.critDmg       // 天赋暴击伤害
```

> 代码注释：暴击倍率 1.5 → 1.8，让暴击更有价值。

### 11.3 暴击应用

```
rawDmg = round(rawDmg × critMult)
```

暴击伤害数字以金色 `#fbbf24` 显示，并附加 `!!` 后缀。

### 11.4 怪物暴击

怪物固定 10% 暴击率，1.8 倍暴击伤害（无加成来源）。

---

## 12. buff/debuff 系统

### 12.1 buff 结构

每个单位（宠物或怪物）拥有独立的 buff 容器：

```javascript
{
  atk: 0, def: 0, spd: 0, all: 0,   // 属性百分比加成
  shield: 0, shieldTurns: 0,         // 护盾值与持续回合
  hotTurns: 0, hotPct: 0,            // 持续恢复（HoT）
  buffTurns: {},                     // 各 buff 的剩余回合
  counterBuff: 0, counterTurns: 0,   // 反击姿态
  reflectBuff: 0, reflectTurns: 0,   // 反射伤害
  defDebuff: 0,                      // 自身防御降低（副作用）
  stolenAtk: 0, stolenAtkTurns: 0    // 偷取的攻击力
}
```

### 12.2 各 buff 说明

| buff 字段 | 作用 | 触发技能示例 |
| --- | --- | --- |
| `atk` | 攻击力百分比加成（`×(1+atk)`） | 攻击强化类 |
| `def` | 防御力百分比加成 | 防御强化类 |
| `spd` | 速度百分比加成 | 加速 |
| `all` | 全属性百分比加成 | 祝福 |
| `shield` | 护盾值（先于 HP 扣除） | 守护盾 |
| `hotTurns` / `hotPct` | 每回合恢复 `maxHP × hotPct` | 生命绽放 |
| `counterBuff` | 受击时反击 `攻击力 × counterBuff` | 反击姿态 |
| `reflectBuff` | 反弹 `受到伤害 × reflectBuff` | 荆棘护盾 |
| `defDebuff` | 自身防御降低百分比 | 狂战之怒副作用 |
| `stolenAtk` | 偷取的攻击力（直接相加） | 偷取类技能 |

### 12.3 buff 回合递减

`applyTurnRegen()` 每回合对所有单位的 `buffTurns` 递减，归零时清除对应 buff：

```javascript
Object.keys(buffs.buffTurns).forEach(k => {
  if (buffs.buffTurns[k] > 0) {
    buffs.buffTurns[k]--;
    if (buffs.buffTurns[k] <= 0) {
      if (k === 'atk') buffs.atk = 0;
      // ...其他字段
    }
  }
});
```

`counterTurns` / `reflectTurns` / `stolenAtkTurns` / `shieldTurns` 独立递减，归零时清除效果。

### 12.4 护盾结算

受到伤害时，护盾优先于 HP 扣除：

```javascript
if (buffs.shield >= monsterDmg) { buffs.shield -= monsterDmg; monsterDmg = 0; }
else { monsterDmg -= buffs.shield; buffs.shield = 0; }
```

---

## 13. 状态系统

### 13.1 状态字段

```javascript
{
  frozen: 0,    stunned: 0,    silenced: 0,
  rooted: 0,    sleeping: 0,
  poisoned: 0,  poisonPct: 0,  poisonStacks: 0,
  burning: 0,   burnPct: 0,
  defReduced: 0, defReduceTurns: 0
}
```

### 13.2 状态效果一览

| 状态 | 效果 | 图标 | 持续递减 |
| --- | --- | --- | --- |
| `frozen` | 冻结，无法行动 | ❄️ | 每回合 -1 |
| `stunned` | 眩晕，无法行动 | 💫 | 每回合 -1 |
| `silenced` | 沉默，无法释放主动技能 | （无图标） | 每回合 -1 |
| `rooted` | 定身，无法行动 | 🌿 | 每回合 -1 |
| `sleeping` | 睡眠，无法行动 | 😴 | 每回合 -1 |
| `poisoned` | 中毒，每回合损失 `maxHP × poisonPct` | ☠️ | 每回合 -1，归零时清空层数 |
| `burning` | 燃烧，每回合损失 `maxHP × burnPct` | 🔥 | 每回合 -1 |
| `defReduced` | 防御降低百分比 | （无图标） | `defReduceTurns` 递减 |

### 13.3 中毒可叠加（stackPoison）

部分毒技能支持叠加：

```
poisonStacks = min(maxStacks || 5, 当前层数 + 1)
poisonPct = (poisonPctPerStack || 0.03) × poisonStacks
```

### 13.4 状态施加深渊

技能通过 `freezeChance` / `stunChance` / `silenceChance` / `rootChance` / `sleepChance` 等字段独立判定每个目标：

```javascript
if (skill.freezeChance && Math.random() < skill.freezeChance) {
  tgtMs.frozen = skill.freezeTurns || 1;
}
```

### 13.5 状态阻止行动

`executePetAction` 与 `executeMonsterAction` 在行动前检查阻止状态：

```javascript
if (status.stunned > 0) { addBattleLog(...); status.stunned--; nextFromQueue(); return; }
// frozen / sleeping / rooted 同理
```

注意：`silenced` **不阻止行动**，仅在 `tryUseActiveSkill` 中阻止技能释放。

### 13.6 净化（cleanse）

治疗技能若带 `cleanse` 字段，可清除目标所有负面状态：

```javascript
st.frozen = st.stunned = st.silenced = st.rooted = st.sleeping = 0;
st.poisoned = st.poisonPct = st.burning = st.burnPct = 0;
```

---

## 14. 被动技能触发机制

### 14.1 主动技能触发概率

`tryUseActiveSkill(pet)` 按技能类型独立判定概率：

| 技能类型 | 基础概率 | 上限 |
| --- | --- | --- |
| 物理攻击 | 8% | 15% |
| 法术攻击 | 20% | 40% |
| 辅助（治疗/buff） | 25% | 50% |

### 14.2 触发加成来源

```
passiveBonus = passives.skillTrigger
             + petStats.skillTrigger       // 天赋法术共鸣
             + bloodline.effects.skillTrigger  // 血统技能触发
triggerChance = baseChance + passiveBonus × 0.5   // 被动加成只生效 50%
```

最终受上限约束。

### 14.3 触发上限

每回合最多检测 **3 个**主动技能：

```javascript
var checkList = available.slice(0, Math.min(3, available.length));
```

且需要满足：未在冷却中、魔法值足够。

### 14.4 连击系统（普攻追加）

普攻 `executeNormalAttack` 中：

| 被动 | 触发概率 | 追加伤害 |
| --- | --- | --- |
| `doubleChance`（连击） | `passives.doubleChance` | 第二段 `damage × 0.7` |
| `tripleChance`（超级连击） | `passives.tripleChance` | 第二段 `damage × 0.7` + 第三段 `damage × 0.5` |

连击在主攻击 400ms 后追加，每段独立判定怪物死亡。

### 14.5 法术连击（magic_double）

`executeActiveSkill` 中，对法术攻击技能追加施法：

```
触发概率 = passives.skillDouble
衰减倍率 = passives.skillDoubleFull ? 1.0 : 0.7   // 超级法连不衰减
```

- 对**同一批目标**追加一次同技能施法。
- 独立判定暴击（使用相同暴击率与暴击伤害公式）。
- 跳过已死目标。

### 14.6 其他被动触发

| 被动字段 | 作用 |
| --- | --- |
| `vampPct` | 吸血：恢复 `damage × vampPct`，暴击时翻倍（需 `vampCritDouble`） |
| `dodgeBonus` | 闪避率加成 |
| `dodgeCounter` | 闪避反击：`攻击力 × 0.5` |
| `parryChance` / `parryReduce` | 格挡概率与减伤比例 |
| `parryCounter` | 格挡反击：`攻击力 × 0.4` |
| `reflectPct` | 反震：`受到伤害 × reflectPct` |
| `reviveChance` / `revivePct` | 神佑复活概率与恢复比例 |
| `regenPct` | 每回合再生比例 |
| `dmgReduce` | 减伤被动 |
| `ignoreDef` | 无视防御比例 |
| `critRate` / `critDmg` | 暴击率 / 暴击伤害加成 |

---

## 15. 光环技能机制

### 15.1 触发规则

`tryExecuteAuraSkills(pet)` 在宠物行动最早期执行（状态检查之后、主动技能之前）：

- 仅处理 `cd` 不存在或为 0 的光环技能。
- **当前回合必释放**：不经过概率判定（100% 触发）。
- 不占用主动技能触发次数。
- 通过 `liveBattle.activeAuras[skill.id]` 标记避免重复释放。

### 15.2 释放流程

```javascript
auraSkills.forEach(skill => {
  if (skill.cd && skill.cd > 0) return;          // 有冷却跳过
  if (liveBattle.activeAuras[skill.id]) return;   // 已生效跳过
  liveBattle.activeAuras[skill.id] = true;        // 标记生效
  executeAuraSkill(pet, skill);                   // 释放
});
```

### 15.3 光环效果

光环效果本身通过 `getAuraEffects(liveBattle.team)` 被动叠加到全队属性。`executeAuraSkill` 负责显示性「释放」动作：日志、动画、效果描述。

支持的光环效果字段（`skill.effect`）：

| 字段 | 作用 |
| --- | --- |
| `teamAtk` | 全队攻击力百分比加成 |
| `teamDef` | 全队防御力百分比加成 |
| `teamSpd` | 全队速度百分比加成 |
| `teamHp` | 全队气血上限百分比加成 |
| `teamCrit` | 全队暴击率加成 |
| `teamRegen` | 全队每回合回复比例 |

### 15.4 显示

光环技能释放时通过 `showSkillCastAnnouncement` 在战场中央显示，颜色为紫色（见 [第 15 节 光环技能机制](#15-光环技能机制) 与 [第 25 节 动画系统](#25-动画系统)）。

---

## 16. 法术群体技能 targetCount 系统

### 16.1 targetCount 字段

技能定义中 `targetCount` 表示「秒 N」（同时命中的目标数）：

| 技能 | targetCount | dmgPct | 类型 |
| --- | --- | --- | --- |
| 陨石天降（meteor） | 3（秒3） | 0.80 | 物理·群体 |
| 烈焰风暴（flame_storm） | 3（秒3） | 0.85 | 物理·群体·持续 |
| 奥术弹（arcane_bolt） | 3（秒3） | 0.15 | 魔法·群体 |
| 暴风雪（blizzard） | 4（秒4） | 0.60 | 法术·群体·控制 |
| 毒雾（poison_mist） | 4（秒4） | 0.50 | 法术·群体·持续 |
| 碧水滔天（water_deluge） | 4（秒4） | 0.55 | 物理·群体 |
| 蝶潮（butterfly_tide） | 5（秒5） | 0.45 | 法术·群体·控制 |
| 佛行震地（buddha_quake） | 5（秒5） | 0.50 | 法术·群体·治疗 |
| 圣光术（holy_light） | 3（秒3） | — | 治疗·单体（治疗生命最低 3 友方） |

### 16.2 目标数越多系数越低

由上表可见，dmgPct 随 targetCount 增加而递减：

```
秒3 → dmgPct ≈ 0.80~0.85
秒4 → dmgPct ≈ 0.50~0.60
秒5 → dmgPct ≈ 0.45~0.50
```

这是平衡设计：目标数越多，单目标伤害越低，总伤害大致持平。

### 16.3 实际作用目标

`executeActiveSkill` 中 AOE 技能按 `targetCount` 限制命中数：

```javascript
if (isAOE) {
  var aliveIndices = getAliveMonsterIndices();
  // 需求19：targetCount 限制（秒3/秒4/秒5），目标数越多伤害系数越低
  if (skill.targetCount && skill.targetCount > 0 && aliveIndices.length > skill.targetCount) {
    // 随机选取 targetCount 个目标
    targetIndices = shuffle(aliveIndices).slice(0, skill.targetCount);
  } else {
    targetIndices = aliveIndices;
  }
}
```

当存活敌人数 > `targetCount` 时，随机选取 `targetCount` 个目标命中；存活敌人数 ≤ `targetCount` 时命中全部。

### 16.4 天赋目标数加成

天赋「智力」3 阶描述：

> 智力+25%, 法术伤害+15%, **法术技能目标数+1（秒3变秒4）**

---

## 17. 战斗速度设置

### 17.1 速度档位（getTurnDelay）

`G.battleSpeed` 控制回合间隔，对应延迟如下：

| 速度档位 | 回合延迟（ms） | 解锁条件 |
| --- | --- | --- |
| 1× | 1200 | 默认 |
| 2× | 600 | 默认 |
| 4× | 250 | 默认 |
| 8× | 125 | 转生 ≥ 1 |
| 16× | 62 | 转生 ≥ 3 |
| 32× | 31 | 转生 ≥ 5 |

```javascript
function getTurnDelay() {
  const speeds = { 1: 1200, 2: 600, 4: 250, 8: 125, 16: 62, 32: 31 };
  return speeds[G.battleSpeed] || 1200;
}
```

### 17.2 速度切换

`setBattleSpeed(speed)` 立即更新 `G.battleSpeed` 并重新调度下一回合（`scheduleNextTurn`）。

### 17.3 UI 表现

速度按钮组在主界面战斗控制区，激活态使用紫色高亮（`.speed-btn.active`），未解锁的档位显示为半透明并标注「需要转生N次解锁」。

---

## 18. 自动战斗

### 18.1 挂机机制

`autoBattleInterval` 是自动战斗的「心跳」标志（实际为 `setInterval(() => {}, 999999)` 的占位定时器，真正驱动靠 `scheduleNextTurn` 的 `setTimeout` 链）。

### 18.2 启动/停止

| 函数 | 作用 |
| --- | --- |
| `toggleAutoBattle()` | 切换挂机状态 |
| `startLiveBattle()` | 启动首场战斗 |
| `stopLiveBattle()` | 清除所有定时器并重置 `liveBattle` |

### 18.3 自动连战

- **主线**：清空一波后 `processWaveCleared` 在 1000ms 后自动 `spawnMonster()` 开启下一波。
- **失败**：`processBattleDefeat` 在 1500ms 后自动重生战斗（损失 5% 经验）。
- **藏宝图**：战斗结束后**停止**自动战斗（`clearInterval(autoBattleInterval)`）。

### 18.4 自动开箱

`G.autoOpenChests` 控制宝箱是否自动开启：

- 开启：`droppedChests.forEach(c => openChestDirect(c))`，直接获得内容物。
- 关闭：宝箱存入 `G.chests` 背包，等待手动开启（可触发转盘动画）。

---

## 19. 主线宝箱掉落（dropChests）

### 19.1 掉落率（getChestDropRate）

| 怪物类型 | 基础掉落率 |
| --- | --- |
| mob | 5% |
| elite | 10% |
| boss | 20% |

实际掉落率受天赋加成：

```
dropRate = base × (1 + getTalentBonus('chest_drop') + getTalentBonus('loot_mastery'))
```

### 19.2 宝箱稀有度

| 怪物类型 | 稀有度判定 |
| --- | --- |
| mob | 80% 白色 / 20% 绿色 |
| elite | 50% 绿色 / 40% 蓝色 / 10% 紫色，掉落 1–2 个 |
| boss | 70% 紫色 / 30% 橙色，固定掉落 1 个 |

### 19.3 宝箱稀有度配置（CHEST_RARITIES）

| 稀有度 | 名称 | 颜色 | 图标 |
| --- | --- | --- | --- |
| white | 白色宝箱 | `#c0c0c0` | ⬜ |
| green | 绿色宝箱 | `#22c55e` | 🟩 |
| blue | 蓝色宝箱 | `#3b82f6` | 🟦 |
| purple | 紫色宝箱 | `#a855f7` | 🟪 |
| orange | 橙色宝箱 | `#fb923c` | 🟧 |

### 19.4 掉落日志

掉落时记录日志：

```
{cr.icon} 掉落了 {cr.name}！
```

---

## 20. 天赋额外掉落

### 20.1 maybeDropTalentLoot(enemyType)

每次击杀怪物后调用，基于天赋概率掉落以下物品：

| 物品 | 基础概率来源 | Boss 加成 | Elite 加成 |
| --- | --- | --- --- | --- |
| 装备 | `equip_drop + loot_mastery × 0.5` | +30% | +10% |
| 融化石 | `fusion_drop + loot_mastery × 0.5` | +50%（2–4 个） | +20%（1–2 个） |
| 孵化石 | `hatch_drop + loot_mastery × 0.5` | +40%（2–3 个） | +15%（1 个） |
| 宝石 | `gem_drop_rate × 0.3` | +8% | +3% |

### 20.2 装备稀有度

```
rarity = 5% 橙色 / 20% 紫色 / 50% 蓝色 / 50% 绿色（分层判定）
```

### 20.3 宝石等级

- 普通掉落：1 级宝石
- Boss 15% 概率掉落 2 级宝石

### 20.4 经验/金币基础奖励

每只怪物击杀奖励（`giveMonsterKillRewards`）：

```
expGain = max(5, (monster.level × 8 + (monster.level - G.player.level) × 5) × (1 + rebirth × 0.2))
goldGain = floor(monster.level × 5 × (1 + rebirth × 0.1))
```

---

## 21. 战斗日志与掉落日志系统

### 21.1 日志类型（addBattleLog）

```javascript
G.battleLog.push({ type, msg, time: Date.now() });
```

支持的 `type` 与 CSS 配色（见 `index.html:397-403`）：

| 类型 | 颜色 | 用途 |
| --- | --- | --- |
| `damage` | `#f87171` 红 | 普通伤害 |
| `heal` | `#4ade80` 绿 | 治疗 |
| `loot` | `#fbbf24` 金 | 掉落 |
| `info` | `#93c5fd` 蓝 | 系统信息（回合开始、阶段切换等） |
| `crit` | `#fbbf24` 金（加粗） | 暴击 |
| `dodge` | `#a78bfa` 紫 | 闪避 |
| `skill` | `#f0abfc` 粉 | 技能释放 |

### 21.2 日志容量

```javascript
if (G.battleLog.length > 100) G.battleLog.shift();
```

最多保留 100 条，超出后丢弃最旧的。

### 21.3 主页掉落日志

主界面日志区标题为「**📦 掉落日志**」，仅显示 `type === 'loot'` 的日志：

```javascript
var lootLogs = G.battleLog.filter(function(l) { return l.type === 'loot'; });
return lootLogs.slice(-40).reverse().map(...);
```

显示最近 40 条掉落日志，倒序排列（最新在最上方，通过 `flex-direction:column-reverse` 实现）。

### 21.4 战斗内日志

`liveBattle.logs` 数组记录当前战斗内的所有日志（包含 damage/skill/crit/dodge/loot/info 等），用于战斗面板实时显示。`liveBattle.logs.unshift(...)` 将最新日志插入头部。

---

## 22. 副本战斗（isDungeon）

### 22.1 副本类型（DUNGEONS）

| 副本 ID | 名称 | 门票 | 奖励 |
| --- | --- | --- | --- |
| exp_cave | 经验洞穴 | exp_ticket | 经验 |
| gold_mine | 黄金矿洞 | gold_ticket | 金币 |
| egg_forest | 蛋之森林 | egg_ticket | 宠物蛋（+1 级） |
| forge_mine | 强化石矿脉 | forge_ticket | 强化石 2–4 颗 |
| treasure_ruin | 藏宝遗迹 | map_ticket | 藏宝图 1–2 张 |
| gem_cavern | 宝石秘洞 | gem_ticket | 宝石 1–3 颗 |
| bloodline_dungeon | 血统副本 | blood_dungeon_ticket | 血统珠 |
| pet_equip_cave | 宠物秘境 | — | 宠物装备 |

### 22.2 副本战斗特性

- `liveBattle.isDungeon = true`
- **单怪模式**：每波 1 个怪物（不使用波次系统）。
- 怪物属性强化：`(40 + lv×20) × 5 × lvScale`（小怪×5 基准）。
- 多波次推进：`dungeonWave` 递增，达到 `dungeonMaxWaves` 后结算奖励。
- 失败时门票消耗且不返还。

### 22.3 副本完成奖励（processDungeonWaveComplete）

每个副本有专属通关奖励（见上表「奖励」列），并触发 `G.statistics.totalDungeons++` 与每日任务 `dungeon_run`。

### 22.4 团本（teamDungeon）

- `liveBattle.dungeonId = 'team_' + dungeonId`
- 每日 1 次（`G.teamDungeonUsed[dungeonId]`）。
- Boss 属性：`(40 + lv×20) × 3 × 4 × lvScale`（精英级 × 团本倍率）。
- 3 波 BOSS，每波掉落随机奖励道具。

---

## 23. 藏宝图战斗（isTreasure）

### 23.1 触发方式

使用背包中的藏宝图道具，进入 `isTreasure: true, treasureMap: tmap` 的特殊战斗。

### 23.2 战斗特性

- **单怪模式**：1 个藏宝图守卫。
- 战斗胜利后调用 `processTreasureRewards()`。

### 23.3 奖励组成（processTreasureRewards）

藏宝图带有词缀（affixes），影响奖励倍率：

| 词缀 ID | 作用 |
| --- | --- |
| `gold_bonus` | 金币加成 |
| `exp_bonus` | 经验加成 |
| `equip_drop` | 装备掉落加成 |
| `egg_drop` | 蛋掉落加成 |
| `skill_drop` | 技能书掉落加成 |
| `moon_dew` | 月华露掉落 |
| `diamond_bonus` | 钻石掉落 |

### 23.4 特殊奖励

- `double_reward`：所有奖励 ×2。
- `guaranteed_equip`：必掉紫色装备。
- `guaranteed_egg`：必掉宠物蛋（T+1 级）。

### 23.5 等级加成

```
levelBonus = 1 + (tmLevel - 1) × 0.02   // 每级 +2% 奖励
```

### 23.6 战斗结束

藏宝图战斗结束后**停止自动战斗**（与主线不同），并弹出 `showTreasureRewardModal(rewards)` 显示奖励列表。

---

## 24. 竞技场战斗（isArena）

### 24.1 字段定义

`liveBattle` 状态中包含 `isArena: false` 字段，与 `isDungeon` / `isTreasure` 并列。

### 24.2 当前实现状态

在 `spawnMonster()` 中，`isArena` 与 `isDungeon` / `isTreasure` 一起被识别为「特殊战斗」：

```javascript
const isSpecialBattle = liveBattle && (liveBattle.isDungeon || liveBattle.isTreasure || liveBattle.isArena);
```

但当前代码库中 `isArena: true` 的实际入口尚未启用（无竞技场入口函数）。该字段作为预留接口存在，未来可扩展为玩家对玩家的镜像战斗。

---

## 25. 动画系统

### 25.1 技能释放公告（showSkillCastAnnouncement）

`ui.js:1295` 实现技能释放时在**战场中央**显示技能名与施法者：

```javascript
function showSkillCastAnnouncement(casterName, skillName, skillType) {
  var color = skillType === 'aura' ? '#a855f7' : skillType === 'active' ? '#f59e0b' : '#3b82f6';
  // 显示 skillName（大号）+ — casterName —（小号）
  // 1500ms 后清空
}
```

### 25.2 技能类型着色

| 技能类型 | 颜色 | 含义 |
| --- | --- | --- |
| `active`（主动） | `#f59e0b` 金色 | 主动技能 |
| `aura`（光环） | `#a855f7` 紫色 | 光环技能 |
| 其他（含被动触发） | `#3b82f6` 蓝色 | 默认色 |

### 25.3 显示结构

```html
<div class="text-center animate-skillCast">
  <p class="text-xl font-black" style="color:{color};text-shadow:0 0 8px {color},0 2px 4px #000;">{skillName}</p>
  <p class="text-sm text-white mt-1">— {casterName} —</p>
</div>
```

### 25.4 怪物生成动画（_rendered 标记）

`renderBattleArena()` 中，每个怪物首次渲染时播放生成动画，**仅首次**：

```javascript
if (isDead) {
  spawnClass = m._deadRendered ? 'opacity-30' : 'animate-monster-die opacity-30';
  m._deadRendered = true;
} else {
  spawnClass = m._rendered ? '' : 'animate-monster-spawn';
  m._rendered = true;
}
```

- `m._rendered`：首次渲染后置 `true`，后续重绘不再播放生成动画。
- `m._deadRendered`：首次死亡渲染后置 `true`，后续保持半透明状态。

### 25.5 死亡动画（_deadRendered 标记）

怪物 HP 归零时：

1. `animateMonsterDeath(monsterIdx)` 给 DOM 添加 `animate-monster-die` 类。
2. 卡片变为 `opacity-30` 半透明。
3. `_deadRendered = true` 防止重复播放。

### 25.6 攻击/受击动画

| 动画类 | 触发场景 |
| --- | --- |
| `animate-pet-attack` | 宠物攻击/施法 |
| `animate-monster-attack` | 怪物攻击 |
| `animate-monster-hurt` | 怪物受击 |
| `animate-pet-hurt` | 宠物受击 |
| `animate-skill-fire/ice/thunder/shadow/heal/poison` | 元素技能特效 |
| `animate-status-poison/burn/freeze/stun` | 状态特效 |
| `animate-shield` | 护盾光环 |
| `animate-monster-spawn` | 怪物生成 |
| `animate-monster-die` | 怪物死亡 |

### 25.7 伤害浮字（spawnDamageFloat）

| 类型 | 颜色 | 字号 | 动画时长 |
| --- | --- | --- | --- |
| 普通伤害 | `#f87171` 红 | 1.1rem | 900ms |
| 暴击伤害 | `#fbbf24` 金 | 1.6rem | 1400ms |
| 治疗 | `#4ade80` 绿 | 1.1rem | 1000ms |
| 闪避 | `#94a3b8` 灰 | 1rem | 800ms |

---

## 26. 参数公式速查表

### 26.1 怪物属性公式

| 类型 | HP | 攻击 | 防御 | 速度 |
| --- | --- | --- | --- | --- |
| 小怪 | `floor((40+lv×20)×5×(1+lv×0.012))` | `floor((4+lv×3.8)×5×(1+lv×0.012))` | `floor(lv×2.0×1.2)` | `floor(lv×2+10)` |
| 精英 | `floor((40+lv×20)×12×(1+lv×0.012))` | `floor((4+lv×3.8)×8×(1+lv×0.012))` | `floor(lv×2.0×1.6)` | `floor(lv×2+10)` |
| Boss | `floor((40+lv×20)×24×(1+lv×0.012))` | `floor((4+lv×3.8)×12×(1+lv×0.012))` | `floor(lv×2.0×2.2)` | `floor(lv×2+10)` |

> 起源草地（mapId=1）的 HP 与攻击再 ×0.5。
> HP 随机波动 ±5%，攻击随机波动 ±10%。

### 26.2 伤害公式速查

| 场景 | 公式 |
| --- | --- |
| 普攻伤害 | `max(1, round((atkPower − def×(0.6−ignoreDef)) × random(0.9,1.1)) × critMult)` |
| 技能伤害（非真实） | `max(1, round((power×dmgPct×skillDmgMult×(1+skillDmgBonus) − def×(0.6−ignoreDefPct)) × random(0.9,1.1)) × critMult)` |
| 真实伤害 | `max(1, round(power×dmgPct×skillDmgMult×(1+skillDmgBonus) × random(0.9,1.1)) × critMult)` |
| 多段伤害第 i 段 | `round(dmg × (1−decayPerHit)^i)` |
| 斩杀加成 | `round(dmg × (executeMult \|\| 2))` |
| 暴击倍率 | `1.8 + passives.critDmg + stats.critDmg` |
| 暴击率 | `0.10 + passives.critRate + auras.teamCrit + bloodline.critRate + skill.bonusCrit + stats.critRate` |

### 26.3 攻击力来源公式

```
物理：atkPower = stats.攻击力 × (1 + rebirth×0.1)
                  × (1 + passives.atkBonus) × (1 + passives.strBonus)
                  × (1 + auras.teamAtk) × (1 + buffs.atk) × (1 + buffs.all)
                  × (1 + bloodline.atkPct) × (1 + bloodline.allPct)
                  × (1 + lowHpAtkBoost×(1−hpPct))   // 低血加成
                  + 角色力量×0.5 + 角色atk
                  + buffs.stolenAtk

法术：spirit = stats.灵力 × (同上大部分加成)
```

### 26.4 触发概率速查

| 触发项 | 概率 |
| --- | --- |
| 物理主动技能 | `min(0.15, 0.08 + passiveBonus×0.5)` |
| 法术主动技能 | `min(0.40, 0.20 + passiveBonus×0.5)` |
| 辅助主动技能 | `min(0.50, 0.25 + passiveBonus×0.5)` |
| 怪物主动技能 | 50%（无需魔法值） |
| 连击（普攻二段） | `passives.doubleChance`，伤害 ×0.7 |
| 超级连击（普攻三段） | `passives.tripleChance`，二段 ×0.7 + 三段 ×0.5 |
| 法术连击 | `passives.skillDouble`，追加施法 ×0.7（或 ×1.0 若 `skillDoubleFull`） |
| 装备绝杀（小怪） | 5%，直接秒杀 |
| 装备格挡 | 10%，伤害减半 |
| 怪物暴击 | 10%，倍率 1.8 |
| 基础闪避 | 5% + `passives.dodgeBonus` + `bloodline.dodgeRate` + `stats.dodgeRate` |

### 26.5 战斗速度延迟对照

| 速度 | 延迟 | 解锁条件 |
| --- | --- | --- |
| 1× | 1200 ms | 默认 |
| 2× | 600 ms | 默认 |
| 4× | 250 ms | 默认 |
| 8× | 125 ms | 转生 ≥ 1 |
| 16× | 62 ms | 转生 ≥ 3 |
| 32× | 31 ms | 转生 ≥ 5 |

### 26.6 宝箱掉落率速查

| 怪物类型 | 基础掉率 | 稀有度分布 | 数量 |
| --- | --- | --- | --- |
| mob | 5% | 80% 白 / 20% 绿 | 1 |
| elite | 10% | 50% 绿 / 40% 蓝 / 10% 紫 | 1–2 |
| boss | 20% | 70% 紫 / 30% 橙 | 1 |

> 实际掉率 = 基础 × (1 + `chest_drop` + `loot_mastery`)。

### 26.7 状态/buff 回合递减规则

| 字段 | 递减方式 |
| --- | --- |
| `frozen/stunned/silenced/rooted/sleeping` | 每回合 -1，行动前检查 |
| `poisoned` | 每回合造成 `maxHP×poisonPct` 伤害，-1，归零时清空层数 |
| `burning` | 每回合造成 `maxHP×burnPct` 伤害，-1 |
| `buffTurns.*` | 每回合 -1，归零清除对应 buff |
| `counterTurns/reflectTurns/stolenAtkTurns/shieldTurns` | 每回合 -1，归零清除效果 |
| `defReduceTurns` | 每回合 -1，归零清除 `defReduced` |
| `hotTurns` | 每回合恢复 `maxHP×hotPct`，-1 |
| 技能冷却 | 每回合 -1 |

### 26.8 关键时序常量

| 时序点 | 延迟 |
| --- | --- |
| 普攻/技能后推进 | 400 ms |
| 连击追加 | 400 ms |
| 自动战斗下一波（主线） | 1000 ms |
| 自动战斗下一波（失败） | 1500 ms |
| 技能释放公告显示 | 1500 ms |
| 伤害浮字（普通） | 900 ms |
| 伤害浮字（暴击） | 1400 ms |
| 闪避浮字 | 800 ms |
| 治疗浮字 | 1000 ms |

---

> **文档来源**：`src/js/battle.js`、`src/js/config.js`、`src/js/ui.js`、`src/index.html`
> **最后更新**：基于当前代码库状态撰写
